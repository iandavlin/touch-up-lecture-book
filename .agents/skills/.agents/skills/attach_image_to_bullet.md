# Skill: Attach image to bullet

You will receive: bullet_text, image_path, mode(optional), md_path(optional), action(optional).

Do the “Task: Attach an image to a markdown bullet (offline)” steps from AGENTS.md.

Defaults:
- md_path = presentation.md
- mode = reference
- action = copy

Return only:
- destination image path
- whether bullet was updated
- whether anchor was appended
- any ambiguity detected